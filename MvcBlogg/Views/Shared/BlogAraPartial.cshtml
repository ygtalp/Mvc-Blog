
    <h5 class="card-header">Aramaya inanmak</h5>
    @using (Html.BeginForm("BlogAra", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <div class="card-body">
            <div class="input-group">
                <input id="Ara" name="Ara" type="text" class="form-control" placeholder="Aramaya inan..." required="required" minlength="3">
                <span class="input-group-btn">
                    <button value="Ara" class="btn btn-secondary" type="submit">Ara !</button>
                </span>
            </div>
        </div>
    }

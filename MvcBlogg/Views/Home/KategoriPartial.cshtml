@model IEnumerable<MvcBlogg.Models.Kategori>
@{

    Layout = null;
}

<h5 class="card-header">Kategoriler</h5>




@foreach (var item in Model)
            {
    <ul class="list-unstyled mb-0">
        <li class="list-group-item">
            <a class="btn btn-dark nav-link" style="color:#fec771" href="/Home/KategoriMakale/@item.KategoriId">@item.KategoriAdi (@item.Makales.Count())</a>
        </li>

    </ul>
}



@*<div class="col-lg-6">
        <ul class="list-unstyled mb-0">
            <li>
                <a href="#">JavaScript</a>
            </li>
            <li>
                <a href="#">CSS</a>
            </li>
            <li>
                <a href="#">Tutorials</a>
            </li>
        </ul>
    </div>*@



@*
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>


    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KategoriAdi)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KategoriAdi)
            </td>
            <td>
                @Html.ActionLink("Düzenle", "Edit", new { id=item.KategoriId }) |
                @Html.ActionLink("Detaylar", "Details", new { id=item.KategoriId }) |
                @Html.ActionLink("Sil", "Delete", new { id=item.KategoriId })
            </td>
        </tr>
    }

    </table>*@
